## 1. 状态机与审核流程重构
- [x] 1.1 将 weekly 审核状态机改为单阶段 `final_review`，移除新的 `outline_review` 进入路径。
- [x] 1.2 保留历史兼容：对历史 artifact / 历史回调动作 `approve_outline` 提供可读兼容提示，不中断流程。
- [x] 1.3 调整 publish 判定、recheck、watchdog、提醒策略在单阶段下的行为与文案。

## 2. 飞书交互与回执升级
- [x] 2.1 更新主审核卡按钮集合，仅保留“终稿通过并发布 / 要求修订 / 拒绝本次”。
- [x] 2.2 `要求修订` 改造为自由文本主输入，并支持可选字段（修订范围、修订意图）。
- [x] 2.3 增加修订失败分型回执与后续动作入口（编辑后重试 / 继续执行 / 直接发布）。
- [x] 2.4 保持幂等去重与异步入队机制，避免重复触发与重复回执。

## 3. ReAct 修订 Agent（受限工具执行）
- [x] 3.1 新增修订 Agent 节点与状态定义（plan -> act -> observe 循环）。
- [x] 3.2 实现 Planner：将自由文本意见拆分为结构化任务列表（严格 JSON schema）。
- [x] 3.3 实现工具白名单（定位、patch、重排、重建、校验、可选补采触发），禁止整文自由重写。
- [x] 3.4 实现多意见处理、冲突检测、低置信度中断与审计 diff 输出。
- [x] 3.5 实现运行护栏：
  - `REVISION_AGENT_MAX_STEPS`（默认 20）
  - `REVISION_AGENT_MAX_WALL_CLOCK_MS`（默认 600000）
  - `REVISION_AGENT_MAX_LLM_CALLS`
  - `REVISION_AGENT_MAX_TOOL_ERRORS`

## 4. 容错与可观测性
- [x] 4.1 Planner/执行器增加重试与退避策略（超时、429/5xx、可修复 JSON 失败）。
- [x] 4.2 输出结构化失败原因分类与修订执行摘要（成功项/失败项/跳过项）。
- [x] 4.3 保证修订失败不阻断主流程，且保持原稿可审阅与可发布。

## 5. 测试与文档
- [x] 5.1 补齐单元测试：单阶段状态机、飞书卡片动作、ReAct 循环、失败分型、兼容分支。
- [x] 5.2 补齐集成测试：从“要求修订”到“重试/继续/直接发布”的闭环。
- [x] 5.3 同步更新 `docs/PRD.md`、`docs/architecture.md`、`docs/learning-workflow.md`。
- [x] 5.4 新增学习会话文档并填写“3 分钟复盘模板”。
