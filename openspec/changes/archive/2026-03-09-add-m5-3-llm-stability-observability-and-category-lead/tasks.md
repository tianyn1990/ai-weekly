## 1. Spec 与设计
- [x] 1.1 明确 M5.3 范围：M5.3a（自适应降载）+ M5.3b（可观测与默认值对齐）+ M5.3c（分类导读）。
- [x] 1.2 明确自适应降载状态机：触发条件、降载策略、恢复条件、兜底行为。
- [x] 1.3 明确新增元数据契约（artifact/schema）：降载触发计数、恢复计数、窗口统计、失败分类与重试统计。

## 2. 工程实现（M5.3a 稳定性）
- [x] 2.1 在 LLM item-wise 执行链路增加短窗口统计（重点监控 missing_content）。
- [x] 2.2 命中阈值时启用临时降载（优先低并发/串行），并对失败条目执行补偿重试。
- [x] 2.3 成功率恢复后自动解除降载，恢复到配置并发。
- [x] 2.4 降载策略失败时继续回退规则摘要，确保 non-blocking。

## 3. 工程实现（M5.3b 可观测与基线对齐）
- [x] 3.1 统一全局并发默认值为 2（CLI 默认、配置示例、Spec/文档口径一致）。
- [x] 3.2 扩展 `llmSummaryMeta`：新增自适应降载与窗口诊断字段。
- [x] 3.3 扩展 warning 文案：输出失败分类、重试统计、降载触发/恢复摘要。
- [x] 3.4 扩展 run 级诊断信息输出（artifact + warning），可快速归因 LLM 失败类型与并发生效状态。

## 4. 工程实现（M5.3c 分类导读）
- [x] 4.1 在报告渲染前生成分类导读（按主要分类生成 1 句导读）。
- [x] 4.2 分类导读失败自动使用模板文案，不阻断报告输出。
- [x] 4.3 更新 markdown 渲染结构，保证导读与正文顺序清晰、对审核友好。

## 5. 测试与验证
- [x] 5.1 单测：覆盖降载触发、恢复、补偿重试、阈值边界、全局并发裁剪。
- [x] 5.2 单测：覆盖分类导读成功/失败回退、渲染输出与结构化产物一致性。
- [x] 5.3 回归：daily/weekly/recheck/watchdog/daemon 不因新增逻辑阻断主流程。
- [x] 5.4 执行 `pnpm test`、`pnpm build`、`openspec validate <change-id> --strict`。

## 6. 文档与学习材料
- [x] 6.1 更新 `docs/PRD.md`：M5.3 目标、配置项、验收项。
- [x] 6.2 更新 `docs/architecture.md`：状态机、容错路径、观测字段。
- [x] 6.3 更新 `docs/learning-workflow.md`：M5.3 阶段目标与交付方式。
- [x] 6.4 新增学习会话文档并填写“3 分钟复盘模板”。
